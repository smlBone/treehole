# 2026-07-16 工作日志

## 完成校园树洞全栈开发

### 后端 (Spring Boot 4.1 + Java 25)
- 创建数据库 schema.sql (users, posts, comments, likes, follows, blocks, messages, system_messages, reports, browsing_history, verification_codes)
- 添加 JWT 依赖 (jjwt 0.12.6) 到 pom.xml
- 实现完整后端架构:
  - config/: WebConfig(CORS+拦截器), RedisConfig, ScheduledTasks(每日登录重置), GlobalExceptionHandler
  - interceptor/: JwtInterceptor (JWT验证)
  - common/: Result, Constants, PageResult, UserContext
  - util/: JwtUtil, DeepSeekChat (小树灵AI)
  - entity/: User, Post, Comment, Follow, Block, Message, SystemMessage, Report
  - dto/: 12个请求/响应DTO
  - mapper/: RowMappers (JdbcTemplate RowMapper)
  - service/: AuthService, UserService, PostService, CommentService, LikeService, MessageService, ReportService, FileService
  - controller/: AuthController, UserController, PostController, CommentController, LikeController, MessageController, ReportController, FileController

### 前端 (Vue 3 + Vite 8)
- 搭建完整前端架构:
  - assets/styles/: variables.css (主题变量), global.css (全局样式)
  - 主题系统: 支持浅色/深色/跟随系统，CSS变量实现
  - stores/: auth, theme, toast (Pinia)
  - api/: index.js (axios实例+拦截器), auth, post, user
  - utils/: crypto.js (SHA-256密码加密), format.js (时间/角色格式化)
  - router/: 路由守卫 (登录/权限控制)
  - components/: AppHeader, PostCard, Avatar, Modal, ReportModal, ToastContainer
  - views/: Login, Register, WorldView, SecretView, PostDetailView, CreatePostView, ProfileView, MessagesView, SettingsView, AdminView, NotFoundView
- 前端构建成功 (125 modules, 467ms)
- 不使用alert/confirm，使用Toast通知组件

### 关键决策
- 使用 JdbcTemplate 而非 JPA/MyBatis（项目已配置 spring-boot-starter-jdbc）
- Jackson 3 使用 tools.jackson 包名（Spring Boot 4.1）
- 前端密码加密使用 Web Crypto API (SHA-256)
- 秘密树洞匿名处理在后端完成（返回时清除userId/authorName）
- 小树灵AI回复异步处理（DeepSeekChat.asyncChat）
